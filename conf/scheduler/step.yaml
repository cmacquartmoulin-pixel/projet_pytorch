# -------------------------------
# conf/model/scheduler/step.yaml
# -------------------------------

_target_: torch.optim.lr_scheduler.StepLR
step_size: 5
gamma: 0.1

# Learning rate schedule (StepLR, step_size=5, gamma=0.1, lr_0=0.001):
# Epochs  1-5  : lr = 0.001 (optimizer initial lr)
# Epochs  6-10 : lr = 0.0001  (x0.1)
# Epochs 11-15 : lr = 0.00001 (x0.1)